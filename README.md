# Colourmaze

Create a maze in which the player has to step from one colour to another
in sequence from start to finish.


## Running the app

Start a local HTTP server, e.g. `python -m http.server 8000`.
Then navigate to `index.html`.


## Running the tests

`node tests/test-generator.js`
