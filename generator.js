// generator.js
// Maze generation logic.

(function (root) {

  // populated in later tasks

  if (typeof module !== 'undefined') {
    module.exports = root;
  } else {
    root.Generator = root;
  }

}({
  generateMaze:    null,
  getLabelColour:  null,
  COLOURS:         null,
}));
